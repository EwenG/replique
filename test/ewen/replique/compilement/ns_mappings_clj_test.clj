(ns replique.compliment.ns-mappings-clj-test
  (:require [replique.repl :refer [tooling-repl]])
  (:import [java.io File]))

(defn my-fn [fff]
  (let [e nil]
    (e))
  (prn "e")
  nil)

(defmacro my-macro [])

::fffffff
